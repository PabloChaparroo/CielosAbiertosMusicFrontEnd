# URL del backend real. El backend ya tiene CORS habilitado para el origin
# de este dev server (FRONTEND_URL en su .env), así que se pega directo acá
# sin necesidad de proxy de Vite.
VITE_API_URL=http://localhost:3000/api
